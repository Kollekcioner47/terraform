ПРАКТИКА 7. KUBERNETES, РЕЕСТР ОБРАЗОВ И SERVERLESS

Продолжительность: 2 академических часа.
Провайдер: yandex-cloud/yandex. Практика выполняется в Яндекс Облаке.

ЧТО БУДЕТ К КОНЦУ ЗАНЯТИЯ

  Вы создадите реестр образов с политикой очистки, кластер Managed
  Service for Kubernetes с группой узлов, подключитесь к нему
  через kubectl, а рядом развернёте serverless-сервис: функцию
  и шлюз API перед ней. Заодно разберёте, какие роли нужны кластеру
  и почему без них он не поднимется.

ЧТО НУЖНО ЗАРАНЕЕ

  * Практики 1–4 (доступ, состояние, модули, машины и группы).
  * Роль storage.editor у сервисного аккаунта — код функции
    загружается в Object Storage.
  * Клиент kubectl на рабочей машине.

ПРЕДУПРЕЖДЕНИЕ ПРО ДЕНЬГИ И ВРЕМЯ

  МАСТЕР КЛАСТЕРА ПЛАТНЫЙ КРУГЛОСУТОЧНО — независимо от того,
  работают ли узлы. И создаётся он 10–15 минут. Поэтому в конфигурации
  кластер выключен переменной create_cluster: включайте его на время
  разбора и удаляйте сразу после.

  Функция и шлюз API стоят копейки: платят за вызовы и время работы.
  Их можно держать включёнными.


========================================================================
ЧАСТЬ 1. БАЗОВАЯ
========================================================================

------------------------------------------------------------------
1. РЕЕСТР ОБРАЗОВ
------------------------------------------------------------------

Приложению в Kubernetes нужен образ. Место, откуда кластер его
забирает, — Container Registry.

  resource "yandex_container_registry" "this" {
    name      = "${var.project_name}-registry"
    folder_id = var.folder_id
  }

  resource "yandex_container_repository" "app" {
    name = "${yandex_container_registry.this.id}/app"
  }

Имя репозитория указывается в формате «идентификатор реестра /
имя репозитория» — именно так, потому что реестр может содержать
много репозиториев, по одному на приложение или на команду.

ПОЛИТИКА ОЧИСТКИ — НЕ РОСКОШЬ, А НЕОБХОДИМОСТЬ

  Хранение образов платное (за гигабайт в сутки). Без политики
  очистки каждый push добавляет слои, и через год реестр становится
  заметной статьёй расходов.

  resource "yandex_container_repository_lifecycle_policy" "app" {
    name          = "${var.project_name}-keep-last"
    repository_id = yandex_container_repository.app.id
    status        = "active"

    rule {
      description  = "Оставлять 5 последних образов с тегами"
      tag_regexp   = ".*"
      retained_top = 5
    }

    rule {
      description   = "Удалять образы без тегов старше недели"
      untagged      = true
      expire_period = "604800s"
    }
  }

  Поля правила стоит запомнить, потому что их часто придумывают
  «по логике» и получают ошибку:

    retained_top   — сколько последних образов оставить;
    expire_period  — через какой срок удалять ("604800s" — неделя);
    tag_regexp     — какие теги подпадают под правило;
    untagged       — удалять ли образы без тегов.

  Отдельного блока для запросов (вроде yql с SQL-выражением) здесь
  нет — мы это проверили на живом стенде.

КАК ЗАГРУЗИТЬ ОБРАЗ

  yc container registry configure-docker

  docker build -t cr.yandex/<registry_id>/app:v1 .
  docker push cr.yandex/<registry_id>/app:v1

  Первая команда прописывает в конфигурацию Docker помощник,
  который подставляет токен облака: пароль вручную не нужен.

------------------------------------------------------------------
2. КЛАСТЕР KUBERNETES: ЧТО НУЖНО, КРОМЕ ЖЕЛАНИЯ
------------------------------------------------------------------

Кластер не создаётся «сам по себе»: у него два сервисных аккаунта,
и у каждого своя работа.

  СЕРВИСНЫЙ АККАУНТ КЛАСТЕРА — от его имени мастер создаёт и удаляет
  ресурсы: диски узлов, адреса, балансировщики. Роли (из документации
  Managed Service for Kubernetes):

    k8s.clusters.agent               управлять ресурсами кластера
    vpc.publicAdmin                  выдавать публичные адреса
    container-registry.images.puller забирать образы из реестра

  СЕРВИСНЫЙ АККАУНТ УЗЛОВ — им пользуются сами узлы: забирают образы,
  подключают диски к подам.

    container-registry.images.puller
    compute.editor
    vpc.user

  Роли выдаются ДО создания кластера, и это надо выразить явно:

    depends_on = [
      yandex_resourcemanager_folder_iam_member.k8s_cluster_agent,
      yandex_resourcemanager_folder_iam_member.k8s_vpc_admin,
      yandex_resourcemanager_folder_iam_member.k8s_nodes_puller,
    ]

  Без depends_on Terraform попробует создать кластер и права
  одновременно, и создание упадёт.

  Отдельно бывает роль kms.keys.encrypterDecrypter — если секреты
  кластера шифруются своим ключом KMS, и logging.writer — если логи
  мастера отправляются в Cloud Logging.

------------------------------------------------------------------
3. ЗОНАЛЬНЫЙ И РЕГИОНАЛЬНЫЙ МАСТЕР
------------------------------------------------------------------

Мастер — управляющая часть кластера (API-сервер, планировщик,
контроллеры). Он бывает двух видов.

  ЗОНАЛЬНЫЙ — один экземпляр в одной зоне:

    master {
      zonal {
        zone      = var.zone
        subnet_id = module.vpc.subnet_ids["k8s-d"]
      }
    }

  Дешевле, но при отказе зоны кластер недоступен (поды продолжат
  работать, а управлять ими будет нельзя).

  РЕГИОНАЛЬНЫЙ — три экземпляра в трёх зонах:

    master {
      regional {
        region = "ru-central1"

        location {
          zone      = "ru-central1-a"
          subnet_id = module.vpc.subnet_ids["k8s-a"]
        }

        location {
          zone      = "ru-central1-b"
          subnet_id = module.vpc.subnet_ids["k8s-b"]
        }

        location {
          zone      = "ru-central1-d"
          subnet_id = module.vpc.subnet_ids["k8s-d"]
        }
      }
    }

  Отказоустойчивый, но дороже в три раза. И требует подсеть
  в каждой из трёх зон — если подсети нет, кластер не создастся.

  В конфигурации выбор сделан переменной, а блоки собраны
  динамическими: так одна конфигурация поддерживает оба режима.

ПУБЛИЧНЫЙ АДРЕС API

    public_ip = true

  Без него kubectl с рабочей машины не подключится. В рабочей среде
  доступ к API чаще закрывают: оставляют внутренний адрес и ходят
  через бастион или VPN.

ОКНО ОБСЛУЖИВАНИЯ И КАНАЛ ОБНОВЛЕНИЙ

    maintenance_policy {
      auto_upgrade = true

      maintenance_window {
        day        = "monday"
        start_time = "03:00"
        duration   = "3h"
      }
    }

    release_channel = "STABLE"

  auto_upgrade разрешает облаку обновлять минорные версии
  в указанное окно. Без окна обновление может прийти в момент
  нагрузки — а минорное обновление Kubernetes перезапускает
  компоненты.

  release_channel: STABLE — проверенные версии, RAPID — новые.
  Для занятий предсказуемее STABLE.

------------------------------------------------------------------
4. ГРУППА УЗЛОВ
------------------------------------------------------------------

Узлы — это обычные виртуальные машины, которыми управляет облако.

  resource "yandex_kubernetes_node_group" "workers" {
    cluster_id = yandex_kubernetes_cluster.this[0].id
    name       = "${var.project_name}-workers"

    instance_template {
      platform_id = "standard-v3"

      resources {
        cores         = var.node_cores
        memory        = var.node_memory
        core_fraction = 20
      }

      scheduling_policy {
        preemptible = true
      }

      boot_disk {
        type = "network-hdd"

        # МИНИМАЛЬНЫЙ РАЗМЕР ДИСКА УЗЛА — 30 ГБ.
        # С меньшим размером группа узлов уходит в бесконечное
        # создание машин (мы это проверили: диск 20 ГБ, 27 минут
        # в статусе CREATING_INSTANCE).
        size = 30
      }

      network_interface {
        nat                = false
        subnet_ids         = [module.vpc.subnet_ids["k8s-d"]]
        security_group_ids = [yandex_vpc_security_group.k8s.id]
      }

      container_runtime {
        type = "containerd"
      }
    }

    scale_policy {
      fixed_scale {
        size = var.node_count
      }
    }
  }

ЧТО ЗДЕСЬ ВАЖНО

  УЗЛЫ БЕЗ ПУБЛИЧНЫХ АДРЕСОВ (nat = false) — так правильно, но им
  нужен выход в интернет, чтобы скачивать образы. Если в сети нет
  NAT-шлюза, поды останутся в состоянии ImagePullBackOff, а узлы
  будут выглядеть здоровыми. В нашей конфигурации модуль сети
  создаёт NAT-шлюз:

    module "vpc" {
      # ...
      create_nat_gateway = true
    }

  ПРЕРЫВАЕМЫЕ УЗЛЫ. Для занятий это экономия, для рабочей нагрузки —
  риск: прерываемый узел может исчезнуть в любой момент, и поды
  на нём перезапустятся. В рабочем кластере узлы обычные.

  ГРУППА БЕЗОПАСНОСТИ обязательна. Узлам нужно общаться друг
  с другом, с мастером и с балансировщиком внутри сети:

    resource "yandex_vpc_security_group_rule" "k8s_internal" {
      security_group_binding = yandex_vpc_security_group.k8s.id
      direction              = "ingress"
      protocol               = "ANY"
      from_port              = 0
      to_port                = 65535
      v4_cidr_blocks         = ["10.90.0.0/16"]
    }

  Правило «всё внутри своей сети» здесь не лень, а необходимость.

  ВТОРОЕ ПРАВИЛО — ДОСТУП К API СНАРУЖИ

  Его легко забыть, и тогда кластер выглядит неработающим:

    resource "yandex_vpc_security_group_rule" "k8s_api" {
      security_group_binding = yandex_vpc_security_group.k8s.id
      direction              = "ingress"
      description            = "Доступ к API Kubernetes"
      protocol               = "TCP"
      port                   = 443
      v4_cidr_blocks         = ["0.0.0.0/0"]
    }

  Мастер доступен по публичному адресу (public_ip = true), но без
  этого правила kubectl с рабочей машины упирается в тайм-аут:

    Unable to connect to the server: dial tcp 130.193.46.202:443:
    connectex: A connection attempt failed because the connected party
    did not properly respond after a period of time

  Мы получили эту ошибку на живом стенде и потратили время на поиск
  причины: кластер был HEALTHY, узлы готовы, а подключаться было
  нельзя. В рабочей среде вместо 0.0.0.0/0 здесь адрес офиса или VPN.

  ПОДКЛЮЧЕНИЕ К КЛАСТЕРУ

    yc managed-kubernetes cluster get-credentials <имя> --external --force
    kubectl get nodes -o wide
    kubectl get pods -A

  Ключ --external означает «настроить kubectl на публичный адрес
  API». Ключ --force перезапишет существующий контекст, если он есть.

  ЧТО ПОЛУЧИЛОСЬ НА НАШЕМ СТЕНДЕ

    kubectl get nodes -o wide
    NAME                       STATUS  AGE    VERSION   INTERNAL-IP
    cl1bmll9kk8p1ffprtde-ihef  Ready   4m44s  v1.35.1   10.90.30.28

    kubectl get pods -A
    kube-system  coredns-...             1/1  Running
    kube-system  kube-proxy-...          1/1  Running
    kube-system  metrics-server-...      1/1  Running
    kube-system  yc-disk-csi-node-v2-... 6/6  Running

  Три вывода из этого вывода:

    * версия Kubernetes — 1.35, её выбрал канал STABLE; версию узлов
      облако подбирает под мастера;
    * операционная система узлов — Ubuntu 22.04, а не та, что
      в семействе образов для обычных машин: управляемые узлы
      собирает облако, и влиять на их ОС нельзя;
    * образы тянутся через NAT-шлюз. Проверить это можно так:

        kubectl create deployment hello --image=nginx:alpine
        kubectl get pods
        NAME                     READY   STATUS    RESTARTS   AGE
        hello-7bd557966d-mn5gk   1/1     Running   0          46s

      Если бы NAT-шлюза не было, под висел бы в ImagePullBackOff.

------------------------------------------------------------------
5. КАК ЭТО ЖЕ ДЕЛАЮТ ГОТОВЫМ МОДУЛЕМ
------------------------------------------------------------------

Создавать кластер вручную — 80 строк конфигурации. Готовый модуль
terraform-yc-kubernetes делает то же самое и вдобавок умеет больше:

  module "kube" {
    # Адрес без суффикса .git — тоже рабочий вариант (проверено).
    source = "git::https://github.com/terraform-yc-modules/terraform-yc-kubernetes?ref=1.1.3"

    network_id = module.vpc.network_id

    master_locations = [
      for s in module.vpc.private_subnets : {
        zone      = s.zone
        subnet_id = s.subnet_id
      }
    ]

    master_maintenance_windows = [{
      day        = "monday"
      start_time = "23:00"
      duration   = "3h"
    }]

    node_groups = {
      "yc-k8s-ng-01" = {
        description = "Основная группа узлов"
        fixed_scale = { size = 3 }
        node_labels = { role = "worker-01", environment = "testing" }
      }
      "yc-k8s-ng-02" = {
        description = "Группа с автомасштабированием"
        auto_scale  = { min = 2, max = 4, initial = 2 }
        node_labels = { role = "worker-02", environment = "dev" }
        max_expansion   = 1
        max_unavailable = 1
      }
    }
  }

ЧТО ЗДЕСЬ СТОИТ ОТМЕТИТЬ

  * источник — git::https с тегом; адрес из документации
    (git@github.com:...) требует SSH-ключа и без него не работает
    (подробно в практике 3);
  * модуль принимает СПИСКИ групп узлов словарём, и это удобнее
    ручного описания: двадцать строк вместо сотни;
  * входные переменные модуля надо смотреть в его README: они шире,
    чем у нашего учебного примера.

  ИСПРАВЛЕНО. В прежней версии курса модули подключались по адресу
  git@github.com:... без указания версии. Это дважды плохо: нужен
  SSH-ключ в GitHub и нет фиксации версии — модуль может измениться
  в любой момент.

------------------------------------------------------------------
6. SERVERLESS: ФУНКЦИЯ И ШЛЮЗ API
------------------------------------------------------------------

Серверные решения описываются тем же Terraform и живут в одной
конфигурации с остальной инфраструктурой.

КОД ФУНКЦИИ ЛЕЖИТ В OBJECT STORAGE

  Провайдер принимает не локальный файл, а ссылку на архив в бакете:

    resource "yandex_function" "api" {
      name       = "${var.project_name}-function"
      user_hash  = filemd5("${path.module}/function/function.zip")
      runtime    = "python312"
      entrypoint = "index.handler"
      memory     = 128

      package {
        bucket_name = yandex_storage_bucket.function_code[0].bucket
        object_name = yandex_storage_object.function_code[0].key
      }
    }

  user_hash — это хэш содержимого архива. Та же логика, что
  с source_hash у объектов и content_base64 у страниц сайта
  (практика 6): без хэша Terraform не заметит, что код изменился.

  Архив собирается ОТДЕЛЬНО, до apply. Готовый архив уже лежит
  в материалах курса (files/07_kubernetes/function/function.zip),
  а пересобрать его после правки кода можно скриптом:

    cd files/07_kubernetes/function
    ./build.sh

  Скрипт соберёт архив и покажет его хэш — тот самый, который
  Terraform сравнивает в user_hash и source_hash. Если утилиты zip
  в системе нет, скрипт соберёт архив средствами Python.

  ВАЖНО: правка index.py сама по себе ничего не изменит. Пока архив
  не пересобран, хэш остаётся прежним, и Terraform считает, что
  код не менялся. Это ошибка «я поправил функцию, применил apply,
  а работает старая версия» — и она встречается в реальной работе
  чаще, чем хотелось бы. Проверить, что архив свежий:

    terraform plan
    # yandex_function.api must be replaced
    #   ~ user_hash = "старый" -> "новый"

  Полезно помнить: тот же принцип у любого кода, который
  загружается в облако архивом или файлом — Docker-образы,
  пакеты слоёв, скрипты инициализации.

ШЛЮЗ API ОПИСЫВАЕТСЯ СПЕЦИФИКАЦИЕЙ OPENAPI

  resource "yandex_api_gateway" "api" {
    name = "${var.project_name}-gateway"

    spec = <<-EOT
      openapi: 3.0.0
      info:
        title: Учебный шлюз API
        version: 1.0.0
      paths:
        /:
          get:
            x-yc-apigateway-integration:
              type: cloud_functions
              function_id: ${yandex_function.api[0].id}
              tag: $latest
              service_account_id: ${yandex_iam_service_account.gateway[0].id}
    EOT
  }

ДВЕ ОШИБКИ, КОТОРЫЕ МЫ ПОЛУЧИЛИ ЗДЕСЬ НА ЖИВОМ СТЕНДЕ

  ПЕРВАЯ: у ресурса yandex_api_gateway НЕТ аргумента
  service_account_id. Авторизация описывается внутри спецификации:

    Error: Unsupported argument
    An argument named "service_account_id" is not expected here.

  ВТОРАЯ: если сервисный аккаунт не указан, шлюз обращается
  к функции анонимно и получает отказ:

    {"message":"request to function '<id>' failed,
     reason: Response code 403 (Forbidden)"}

  Ошибка выглядит как ошибка в коде функции, хотя дело в правах.
  Сервисному аккаунту шлюза нужна роль на вызов функции.

  И ТРЕТЬЯ, МЕНЕЕ ОЧЕВИДНАЯ: имя роли. Логичное на вид
  functions.invoker облако не принимает:

    Error: Failed to Update IAM Policy
    Role 'functions.invoker' not found

  Правильное имя — functions.functionInvoker.

ПРОВЕРКА

  terraform output -raw api_gateway_domain
  https://d5d7f41fbrj8lv4qqgm6.0ly8ed4d.apigw.yandexcloud.net

  curl -s https://d5d7f41fbrj8lv4qqgm6.0ly8ed4d.apigw.yandexcloud.net/
  {
    "message": "Функция создана Terraform",
    "course": "terraform",
    "lesson": "7",
    "request_id": "52e2fe0a-cc7d-4510-8a1d-11dda595a64c",
    "user_agent": "curl/8.21.0",
    "event_keys": ["body", "headers", "httpMethod", ...]
  }

  Прямой вызов функции командной строкой:

    yc serverless function invoke <function_id>

  Здесь важно одно: при прямом вызове событие (event) приходит
  ПУСТЫМ. Функция, которая сразу обращается к event["headers"],
  падает:

    {"errorMessage": "'NoneType' object has no attribute 'get'",
     "errorType": "AttributeError"}

  Мы получили эту ошибку на стенде и добавили в код строку
  event = event or {} — правило «функция обязана пережить пустой
  запрос» полезно помнить.


========================================================================
ЧАСТЬ 2. ПРОДОЛЖЕНИЕ
========================================================================

------------------------------------------------------------------
7. ЧТО СТОИТ И ЧЕГО НЕ СТОИТ ДЕЛАТЬ В KUBERNETES ЧЕРЕЗ TERRAFORM
------------------------------------------------------------------

Вопрос, который возникает на каждой второй консультации: «мы должны
описывать в Terraform всё, что внутри кластера?»

Ответ: нет, и вот почему.

  В TERRAFORM ОПИСЫВАЮТ:
    * сам кластер и группы узлов;
    * сеть, подсети, группы безопасности;
    * реестр образов и права доступа к нему;
    * сервисные аккаунты и роли;
    * балансировщики, если они живут вне кластера.

  ВНУТРИ КЛАСТЕРА ОБЫЧНО ОПИСЫВАЮТ ДРУГИМИ ИНСТРУМЕНТАМИ:
    * Helm-чартами — приложения;
    * манифестами и kustomize — объекты Kubernetes;
    * Argo CD или Flux — то, что разворачивается непрерывно.

  Причина простая: изменения внутри кластера происходят десятки раз
  в день (новые версии образов, правки конфигураций), а изменения
  инфраструктуры — раз в недели. Один конвейер для этих двух
  скоростей неудобен: план Terraform будет каждый день показывать
  одно и то же.

  Практическое правило: Terraform создаёт кластер, а что внутри
  кластера — дело инструментов, которые для этого предназначены.
  Кстати, есть провайдер Kubernetes для Terraform, и им пользуются,
  когда объектов мало и они меняются редко.

------------------------------------------------------------------
8. СКОЛЬКО ЭТО СТОИТ
------------------------------------------------------------------

Полезно посчитать до занятия, потому что Kubernetes — самая дорогая
часть курса.

  МАСТЕР КЛАСТЕРА — платный круглосуточно. Зональный дешевле
  регионального в три раза.

  УЗЛЫ — платят как за виртуальные машины. Прерываемые дешевле,
  core_fraction = 20 дешевле, чем 100.

  БАЛАНСИРОВЩИКИ внутри кластера создаются автоматически при
  использовании сервисов типа LoadBalancer — это тоже расход.

  РЕЕСТР ОБРАЗОВ — за хранение. Отсюда политика очистки.

  ФУНКЦИЯ И ШЛЮЗ API — за вызовы. Для занятий это единицы рублей.

Отсюда три привычки:

  1. Не держать кластер включённым между занятиями.
  2. Использовать прерываемые узлы и минимальные пресеты.
  3. Удалять реестр, если образы в нём больше не нужны.

------------------------------------------------------------------
9. РАЗБОР ПОЛОМОК
------------------------------------------------------------------

  СИМПТОМ: кластер не создаётся, ошибка про права сервисного аккаунта
  ПРИЧИНА: у сервисного аккаунта кластера нет ролей k8s.clusters.agent
  и vpc.publicAdmin, либо роли выданы одновременно с созданием
  кластера.
  ЧТО ДЕЛАТЬ: проверить роли и добавить depends_on на ресурсы выдачи
  прав.

  СИМПТОМ: узлы создаются, но поды висят в ImagePullBackOff
  ПРИЧИНА 1: узлам не хватает прав: нужна роль
  container-registry.images.puller сервисному аккаунту узлов.
  ПРИЧИНА 2: у узлов нет выхода в интернет (нет NAT-шлюза при
  nat = false).
  ЧТО ДЕЛАТЬ: проверить роли и маршрут в интернет — так же, как
  в практике 4.

  СИМПТОМ: apply «висит» на создании группы узлов, узлы не появляются
  ПРИЧИНА: размер загрузочного диска узла меньше минимального.
  В списке инстансов группы машин видно точную причину:

    yc compute instance-group list-instances <instance_group_id>
    STATUS: CREATING_INSTANCE [27m]
    [INVALID_ARGUMENT] Request validation error:
    Disk size must be greater or equal than 30.0 GB

  Terraform при этом не падает — он ждёт готовности группы.
  ЧТО ДЕЛАТЬ: поставить размер диска 30 ГБ или больше. Мы потеряли
  на этом больше получаса: сообщение об ошибке видно только
  в состоянии группы машин, а не в выводе Terraform.

  СИМПТОМ: kubectl не подключается к кластеру
  ПРИЧИНА 1: не выполнен yc managed-kubernetes cluster
  get-credentials.
  ПРИЧИНА 2: у мастера нет публичного адреса (public_ip = false),
  а вы подключаетесь снаружи.
  ПРИЧИНА 3: группа безопасности кластера не пускает трафик
  на порт 443 снаружи. Это самая частая причина: кластер HEALTHY,
  узлы готовы, а kubectl получает тайм-аут.
  ЧТО ДЕЛАТЬ: проверить контекст kubectl (kubectl config
  get-contexts), публичный адрес и правила группы безопасности.

  СИМПТОМ: шлюз API отвечает 403 при вызове функции
  ПРИЧИНА: в спецификации не указан service_account_id интеграции,
  либо у сервисного аккаунта нет роли functions.functionInvoker.
  ЧТО ДЕЛАТЬ: добавить параметр в спецификацию и выдать роль.

  СИМПТОМ: Error: Role 'functions.invoker' not found
  ПРИЧИНА: неверное имя роли. В Яндекс Облаке оно пишется
  functions.functionInvoker.
  ЧТО ДЕЛАТЬ: сверять идентификаторы ролей в документации сервиса,
  а не придумывать по аналогии.

  СИМПТОМ: функция падает с AttributeError про NoneType
  ПРИЧИНА: при прямом вызове событие пустое, а код сразу обращается
  к полям события.
  ЧТО ДЕЛАТЬ: начинать обработчик с event = event or {}.

  СИМПТОМ: изменения кода функции не применяются
  ПРИЧИНА: у функции не обновился user_hash, или архив не перезалит
  в бакет.
  ЧТО ДЕЛАТЬ: пересобрать архив и проверить, что user_hash
  вычисляется от файла (filemd5), а не вписан руками.

  СИМПТОМ: Error: Unsupported argument для правила политики очистки
  ПРИЧИНА: придуманное поле (например, блок с запросом).
  ЧТО ДЕЛАТЬ: сверять поля правила по схеме провайдера:
  terraform providers schema -json.

------------------------------------------------------------------
10. ЗАДАНИЯ
------------------------------------------------------------------

  1. Реестр. Соберите минимальный образ (например, nginx со своей
     страницей), загрузите его в реестр и убедитесь, что политика
     очистки удаляет старые теги. Подсказка: политику можно
     запустить вручную через yc.

  2. Кластер. Переключите master_type на regional и посмотрите план.
     Ответьте: чем региональный мастер отличается по стоимости
     и по требованиям к сети?

  3. Узлы. Добавьте вторую группу узлов с автоматическим
     масштабированием и метками для планировщика. Убедитесь, что
     поды можно размещать на конкретной группе.

  4. Приложение. Разверните в кластере любое приложение через
     Helm и убедитесь, что образ берётся из вашего реестра.

  5. Функция. Добавьте в функцию второй путь (/health), который
     возвращает только статус. Опишите его в спецификации шлюза.

  6. Права. Уберите роль functions.functionInvoker у сервисного
     аккаунта шлюза и проверьте, что шлюз начнёт отвечать 403.
     Верните роль.

  7. Экономика. Посчитайте, во что обойдётся этот стенд за сутки
     непрерывной работы, и предложите три способа сократить расходы,
     не потеряв работоспособность.

------------------------------------------------------------------
11. ЧТО ДАЛЬШЕ
------------------------------------------------------------------

Инфраструктура собрана: сеть, машины, данные, Kubernetes и serverless.
В практике 8 займёмся тем, без чего это всё не живёт в рабочей среде:
разделением окружений, конвейером с планом в pull request, тестами
конфигурации и итоговым стендом, который разворачивается одной
командой.
